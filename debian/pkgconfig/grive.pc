prefix=/usr
libdir=${prefix}/lib
includedir=${prefix}/include/grive

version = 0.1.0

name = grive
pkgname = libgrive0

Name: ${name}
Version: ${version}
Description: Grive library version 0.1.0
Cflags: -I${includedir}
Libs: -L${libdir} -lgrive
